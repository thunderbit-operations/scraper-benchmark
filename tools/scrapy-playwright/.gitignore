# Never commit the local virtualenv (201 MB; recreate via the README).
.venv/
venv/
__pycache__/
*.pyc
.pytest_cache/

# Run logs contain absolute local paths — the JSON summaries carry the numbers.
*.log
artifacts/logs/

# OS noise
.DS_Store
