# Never commit the local virtualenv (recreate via the README).
.venv/
venv/
__pycache__/
*.pyc
.pytest_cache/

# Run logs may carry absolute local paths — the JSON summaries carry the numbers.
*.log
*.out
*.stdout
artifacts/logs/

# Katana resume checkpoints are per-run local state.
resume-*.cfg

# OS noise
.DS_Store
